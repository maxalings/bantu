  <%=  button_to service_favorites_path(service), class: "fav-icon p-0",
  data: { controller: "favorites", action: "click->favorites#toggle", url: service_favorites_path(service) } do %>
    <% if current_user.favorites.exists?(service: service) %>
      <i class="fa-solid fa-heart" data-favorites-target="icon"></i>
    <% else %>
      <i class="fa-regular fa-heart" data-favorites-target="icon"></i>
    <% end %>
  <% end %>

<%# problem here -> GET instead of POST (look for a favorite index) whereas post is defined %>
